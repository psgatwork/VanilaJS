const numR = [12, 94, 24, 75, 33, 74, 67, 28, 29, 36];

// This iterates all elements but returns nothing
numR.forEach((x) => console.log(x + 2));

// What is the difference between every(), foreach() and map()?
// .map() returns a new Array of objects
// .every() returns a boolean
// .forEach() returns nothing
